## No Specification Changes

This change is a dependency and tooling upgrade only. The bundle's runtime behavior, interfaces, and service configuration are unchanged between v1 and v2. No new, modified, or removed requirements.

See proposal.md for the full scope of changes (all at the build/CI/dependency level).
